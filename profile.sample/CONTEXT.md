# Curation Context — <YOUR NAME / CONTEXT> (template)

> The `mix` and `profile` skills read this file at session start: who you're building for,
> what you actually like, and what must never show up. Fill in the sections below; delete any
> that don't apply — the skills no-op gracefully when a section is empty.

## Who I am

<Name, and what this context is for. "Cory, personal library" vs "office background music"
are different overlays if you want them kept apart.>

## Taste

<What you reach for. Genres, eras, the texture you want. Be concrete — "mid-tempo, warm,
90-110bpm, guitar-forward" beats "good music".>

## Hard vetoes

<Artists, genres, or moves that must never appear in a generated mix. These are absolute —
the skills treat this list as a filter, not a preference. Example:>

- <artist you never want to hear>
- <"no ballad wind-down at the end">

## House rules

<How mixes should be built for you. Examples of the kind of rule that belongs here:>

- Save first (private), then show the tracklist and ask for changes.
- Favor deep cuts over hits — popularity is a negative signal.
- Source mainly from my own playlists; go light on editorial/algorithmic pools.
- Participate in cooldown: build with `roster --fresh`, ship with `create --cooldown`.

## People I build for

<Short pointers to the friends/family you make mixes for, and where their full profile lives.
The `profile` skill writes detailed profiles to agent memory; this is just the index. Example:>

- <name> — <one-line taste summary> · profile in memory as `<slug>`

## Notes

`notes/` is your scratch space for this context — backlog, ideas, open questions.
`playlists.md` is the pool cheat sheet: what each playlist is for and how much to trust it.
